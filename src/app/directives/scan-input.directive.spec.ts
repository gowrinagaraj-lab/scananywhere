import { ScanInputDirective } from './scan-input.directive';

describe('ScanInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ScanInputDirective();
    expect(directive).toBeTruthy();
  });
});
